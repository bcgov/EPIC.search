# Docker Compose Override for Local Development
# Copy this file to the root directory as docker-compose.override.yml.example
# This file contains local development values and should NOT be committed to source control

version: "3.9"

services:
  epic-search-api:
    environment:
      # S3 Configuration (replace with your actual values)
      - S3_BUCKET_NAME=your-actual-bucket-name
      - S3_ACCESS_KEY_ID=your-actual-access-key
      - S3_SECRET_ACCESS_KEY=your-actual-secret-key
      
      # Local development CORS
      - CORS_ORIGIN=*

# Note: Database and other settings use defaults from main docker-compose.yml
# which are suitable for local development
